{
  "buildCommand": "cd FlowState && npm run build",
  "outputDirectory": "FlowState/dist",
  "installCommand": "npm install && cd FlowState && npm install",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  }
}
